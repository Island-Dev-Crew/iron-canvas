# IMMERSIVE Mode — Iron Canvas v4.3

> **Agent F + IMMERSE 4.5 + Evidence Gate.** Use when the brand moment requires a real 3D/WebGL/advanced-motion centerpiece, not just polish.

> **v5.0 note:** IMMERSIVE is now the evidence-gated execution lane of the **Depth Language**
> (DIS System 9 — `references/depth-language.md`, SKILL.md §20/§22). It AUTO-SELECTS when
> depth intensity resolves ≥ 0.4. Sustained Tier III work dispatches `surfaces/immersive-3d/`.
> This document's doctrine and gate are unchanged and authoritative.

IMMERSIVE is not a fourth speed tier. It is a **quality mode** layered on top of SWARM or MISSION when the Phase 2 Brand Personality Matrix and Phase 3/3.9 Design PRD justify frontier motion: WebGL, React Three Fiber, Three.js, particles, shader-like atmospherics, cinematic load sequences, Remotion/Flow assets, or browser-driven visual evidence.

The rule is simple: **do not call it immersive until the evidence gate passes.**

---

## When to Select IMMERSIVE

Select IMMERSIVE when at least 3 are true:

- Bold ≥ 8 or Avant-garde ≥ 8 in the Brand Personality Matrix
- The project needs one unforgettable hero/signature moment
- The visual story benefits from depth, parallax, particles, or 3D materials
- Performance tier allows progressive enhancement / WebGL fallback
- The project is flagship, launch-critical, investor-facing, or brand-defining
- There is enough time to run screenshot, console, mobile, reduced-motion, and fallback checks

Do **not** select IMMERSIVE for simple brochure pages, text-first public notices, speed-critical SEO pages, or brands whose DNA is deliberately quiet/minimal.

---

## Pipeline Insertion

```text
ORIENT(0) → INTERVIEW → STUDY(1) → FEEL(2) → SCOUT(3) → VALIDATE(3.5) → PACKAGE(3.9)
→ FORGE(4) → IMMERSE(4.5 / Agent F) → GENERATE(5) → COMPOSE(6) → VERIFY(7) → HANDOFF(8)
```

Agent F owns the immersive layer only. Agents A/B/C/D/E keep their domains:

- Agent A: foundation/tokens/semantic structure
- Agent B: GSAP/Lenis/scroll choreography
- Agent C: UI components/interactions
- Agent D: generated/code-driven assets
- **Agent F: WebGL/3D/immersive hero/signature scene + evidence harness**
- Agent E: final QA and enhancement discovery

---

## Acceptance Gate

IMMERSIVE passes only when:

- `templates/immersion-scorecard.md` is filled for the project
- Average score is **≥ 4.0 / 5.0**
- Automatic failures are zero:
  - No console/page errors
  - Mobile has no horizontal overflow
  - Reduced-motion path is static/clean
  - WebGL/3D has a static fallback
  - One-line kill switch exists for the immersive layer
  - Evidence screenshots exist for desktop + mobile + reduced-motion + tune/config surface when applicable

---

## Required Artifacts

Every IMMERSIVE project should leave behind:

- `src/components/.../config.ts` or equivalent single control surface
- `?tune` or documented local tuning path when live tuning is possible
- Playwright smoke/evidence harness
- Desktop/mobile screenshots
- Filled scorecard
- Remaining risks section
- Kill-switch path documented in handoff

---

## Standing Doctrine

- One unforgettable immersive moment beats ten scattered effects.
- The immersive layer must amplify the site DNA, not impose Iron Canvas style.
- WebGL must be progressive enhancement, never the only usable experience.
- Motion must honor `prefers-reduced-motion`.
- Evidence beats adjectives. Premium is a score, not a vibe claim.

*Iron Canvas v4.3 — Where there is no vision, the people perish. Proverbs 29:18 (KJV)*
