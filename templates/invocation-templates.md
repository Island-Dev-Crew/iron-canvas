# Invocation Templates — Iron Canvas v6

> Copy-paste these to invoke Iron Canvas in Antigravity, Claude Code,
> or any agentic AI environment. Every template references the correct phases,
> agents, and gates.

---

## ★v6 INVOCATIONS (SKILL.md §15)

```
# FROM AN IDEA — the Studio front door ★v6
Open studio/index.html, write the idea, subject, mechanism and feel line, pick the register,
surface, personality and depth, switch on the engines you have — then paste the compiled
mission into Claude Code:
  "Run Iron Canvas v6 on this production bible. …"   (the bible's decisions are recorded at ORIENT)

# MISSION MODE — Full team
Iron Canvas v6 MISSION MODE for [project].
Full team. Discovery Interview first (Q8 = the mechanism). Phases 0-8 + PREMIERE.
Target: Awwwards Site of the Day quality — alive at rest, one signature, one clock.

# SWARM MODE — Standard production
Iron Canvas v6 SWARM MODE for [URL/project].
Phases 0-3.9 sequential (the score + preflight at 3.9). Then parallel agents ‖ ASSET FORGE.
Merge and verify (7 axes).

# SOLO MODE — Quick enhancement
Iron Canvas v6 SOLO for [URL].
Phase 1 FIRST — no changes before DNA profile. The aliveness floor still ships.

# DASHBOARD BUILD
Iron Canvas v6 MISSION MODE — surfaces/app-dashboard for [name].
Type C project. Bento archetypes (perpetual states) + information design + data animation.

# WORLD — Tier III
Iron Canvas v6 — surfaces/immersive-3d for [name]. Agent F. Camera rail = the score's clock.
Engines: Blender clay rail → Seedance @Video1 → the same rail in WebGL (the twin camera).

# SCROLL SEQUENCE — Keyframe Interpolation
Iron Canvas v6 Phase 5 scroll sequence for [site].
Method: start + end frames → Flow or Seedance 2.5 (end_image_url) → 15fps extraction → ledger.
Product: [description]. Section: [where, dimensions].

# PREMIERE — the launch film
Iron Canvas v6 Phase 8 PREMIERE for [project]: render the page's score with HyperFrames
(seeked, deterministic) + Seedance shots for photoreal beats. Contact sheet, then render.

# HANDOFF
Iron Canvas v6 Phase 8 HANDOFF for [project]. Generate HANDOFF.md.
```

The longer templates below carry the same v6 gates: the Feel Brief with a kinetic signature at
Phase 2, the score + Artifact Assessment Gate + preflight at 3.9, ASSET FORGE (5a) in parallel with
FORGE, the media tournament at Phase 5, and 7-axis VERIFY — Axis 7 Aliveness is never waived.

---

## SOLO MODE — Full Sequential Pipeline

```
Apply Iron Canvas v6 SOLO to [URL/project].

Read SKILL.md first, then execute phases sequentially:
Phase 0 (ORIENT): Classify project type and mode. Discovery Interview (Q8 = the mechanism).
                  Power-engine scan (node engines/detect.mjs).
Phase 1 (STUDY): DNA profile — do NOT change anything before this is complete.
Phase 2 (FEEL): The Feel Brief — lens, reference vibe, feel line, kinetic signature
                + Brand Personality Matrix.
Phase 3 (SCOUT): Direction Fusion (Voice · World · Instrument) + generate North Star image with Nano Banana Pro.
Phase 3.5 (VALIDATE): Verify all planned features for technical feasibility.
Phase 3.9 (PACKAGE): Write the Design PRD using templates/design-prd.md + the score (score.json)
                     + the Artifact Assessment Gate and engine jobs. scripts/ic-preflight.mjs must pass.
Phases 4-6 (FORGE ‖ ASSET FORGE → GENERATE → COMPOSE): Build it — perform the score; judge the media.
Phase 7 (VERIFY): 7-axis audit. Do not deliver until all axes pass (Axis 7 Aliveness is never waived).

SOLO caps how many systems activate — never the aliveness floor (arrival, heartbeat, hand-feel,
breath, composed still). Output all JSON/MD files to project root. Final deliverable only after verification.
```

---

## SWARM MODE — Parallel Build Agents

```
Apply Iron Canvas v6 SWARM to [URL/project].

Orchestrator: Run Phases 0-3.9 sequentially. Produce design-prd.md + score.json
(preflight PASS) + artifact-assessment.json with the engine jobs.

Then dispatch to parallel Build Agents simultaneously (ASSET FORGE 5a runs beside them):
  Agent-A (Foundation): Section 3 of PRD → tokens.css, index.html, base.css (act ids, data-ic, data-breath)
  Agent-B (Motion): Section 4 of PRD → performs the score: motion.js + canvas-score.js, [scroll-engine.js]
  Agent-C (UI/UX): Section 5 of PRD → components.css, interactions.js (hand-feel on every control)
  Agent-D (Artifacts): Section 6 of PRD → generated images, frames/, engine jobs → the ledger
  Agent-E (QA): Review all agent outputs → qa-fixes.md + the aliveness evidence

Orchestrator: Phase 5 media tournament, merge agents (A→B→C→D→E), then Phase 7 verification (7 axes).
Target: Awwwards Site of the Day quality (7.5+ on all axes) — alive at rest, one signature, one clock.
```

---

## MISSION MODE — Full Team

```
Iron Canvas v6 MISSION MODE for [project].

Full agent team:
  Research Agent → Phases 1-3 (DNA + the Feel Brief + Direction Fusion + North Star)
  Validation Agent → Phase 3.5 (cross-LLM technical check)
  Orchestrator → Phase 3.9 (Design PRD + the score + engine jobs; preflight; dispatch)

  Parallel Build Agents (simultaneous after PRD dispatch — ASSET FORGE 5a runs beside them):
    Agent-A: Foundation (tokens, HTML, semantic structure, score hooks)
    Agent-B: Motion (performs the score — GSAP, scroll engine, page transitions, WebGL)
    Agent-C: UI/UX (components, cursor, micro-interactions, hand-feel)
    Agent-D: Artifacts (AI image generation, scroll sequences, power engines → the ledger)
    Agent-E: QA (accessibility, performance, cross-browser, aliveness evidence)
    Agent-F: Immersion (when depth ≥ 0.4 — subscribed to the score's clock)

  Orchestrator: Media tournament, merge, compose, run 7-axis verification audit, PREMIERE.

Target: Awwwards Site of the Day. Taste Test 7/7. Performance ≥ 10/12. Aliveness 6/6.
Deliver only after all 7 verification axes pass.
```

---

## Dashboard Build — Mission Control Route

```
Iron Canvas v6 MISSION MODE — DASHBOARD ROUTE for [name].

Project type: C (Dashboard/Mission Control). Dispatch surfaces/app-dashboard/PACK.md
(it incorporates the § DASHBOARD ADDENDUM in SKILL.md).

Phase 3 SCOUT: Search specifically for:
  "mission control UI dark dashboard premium 2024"
  "dark analytics bento grid glassmorphism premium"
  SpaceX Dragon UI, NASA mission control, Bloomberg Terminal concepts.
Generate North Star from the palette DERIVED from this brand's DNA ([ground hex] + [accent hex]) —
light, dark or both is the brand's call, never the studio's obsidian + gold — bento cards of
varying size, one live chart as the centrepiece.

Phase 4 FORGE: Bento grid system, card tokens, data animation patterns.
Agent-B: Counter animations, chart entry animations, live pulse effects —
the five bento archetypes keep a quiet perpetual state (alive, not busy).
No scroll engine. No WebGL background. No cursor trail.
Data: [describe what metrics/data are being displayed].
```

---

## Existing Site Enhancement

```
Iron Canvas v6 SOLO — Enhancement of [URL].

Project type: E. Phase 1 STUDY is NON-NEGOTIABLE and must complete before any changes.

Phase 1: Screenshot every section desktop + mobile. Extract all CSS hex codes.
         Map every font. List every image. Document strengths AND weaknesses.
         Complete site-dna-profile.json BEFORE proceeding.

Phase 2: Discover feel from the site itself — do not impose. Write the Feel Brief
         (lens, reference vibe, kinetic signature) from what the site already is.
Phase 3: Scout references that MATCH this site's feel (not generically premium).
Phase 4: Enhance within the existing DNA — never impose a template.
         Universal upgrades: reveals, 3-state interactions, tinted shadows, tokens —
         and the aliveness floor (arrival, heartbeat, hand-feel, breath, composed still).
Phase 7: "Is this recognizably the SAME site but better?" If no, pull back.
         Axis 7: is it alive at rest? If no, it is not better yet.

Anti-Pattern priority for this project: #5 Identity Erasure is highest risk.
```

---

## E-commerce Scroll Sequence

```
Iron Canvas v6 SWARM for [site] with scroll-driven product reveal.

Product: [description].
Scroll section: [section name, current dimensions].

Phase 3 SCOUT: Reference Apple.com, DJI product pages, Codrops product scroll sequences.
Phase 3.9 PACKAGE: the reveal is the score's signature act (scrub, pinned, hold on the final frame).
Phase 5 GENERATE (Agent-D):
  - Pre-generation checklist mandatory before any prompt.
  - Define 10 keyframe moments: sealed → glow → lid lift → open → reveal → hero —
    more frames at the critical moment, fewer in the settle.
  - Base prompt: [product] + [material/palette hex] + [studio lighting] + [bg] + [style]
  - VARY ONLY the action descriptor per frame.
  - Generate 4 variants per keyframe. A blind critic selects — with a written reason (ledger).
  - TEST 10 frames with scroll engine first → then generate full 60-frame set.
  - Frame specs: WebP, 1920×1080 desktop, 960×540 mobile, ≤ 80KB each.

Phase 6 COMPOSE (Agent-B):
  - Canvas + GSAP ScrollTrigger scroll engine (NEVER <video>) — one clock: the score's scrub act.
  - Section height: 500vh.
  - Content overlays at scroll progress: [list text + percentages].
  - Fallback: static hero if frames fail to load; reduced motion rests on the final frame.
```

---

## Narrated Scroll Experience (Voice AI)

```
Iron Canvas v6 SWARM for [site] with scroll-triggered narrated experience.

Sound doctrine (v6): narration is invited, never imposed — off by default behind a visible
toggle, remembered per visitor; the scroll cues speak only after the visitor turns it on.
No autoplay, ever.

Phase 3.5 VALIDATION AGENT — verify before building:
  1. WebSocket TTS streaming feasible in browser (HTTPS required)?
  2. AudioContext user-gesture requirement — how to handle?
  3. Scroll-sync latency budget: [TTS latency] vs scroll trigger lead time.
  4. Web Speech API as fallback if TTS API unavailable?

If FEASIBLE, dispatch to build agents:

Agent-B: Implement useFalcon.ts (or equivalent) hook:
  - WebSocket connection on page load
  - speak(text) function triggering at scroll positions from PRD
  - scheduleAudioChunk() using AudioContext nextPlayTime for gapless playback
  - Pre-trigger: send TTS request 300ms before scroll visual fires
  - AudioContext resume() inside prior user click event

Agent-C: Floating barista/narrator UI component:
  - Fixed position bottom-right
  - States: idle (collapsed) → listening → thinking → speaking
  - Luxury theme matching site DNA (colors from tokens.css)
  - Waveform animation during speaking state (CSS or Lottie)

Voice engine: Murf Falcon / ElevenLabs / Web Speech API
Narration moments (from scroll positions):
  - [text] at [scroll % or section name]
```

---

## Artifacts Only (Phase 5 Focused)

```
Iron Canvas v6 Phase 5 — Context-aware artifact generation for [site/project].
Generate for the context, not for the prompt.

Prerequisite: site-dna-profile.json, feel-profile.json and artifact-assessment.json
(the gate runs at PACKAGE 3.9b) must exist before starting.

For EACH artifact needed:
  1. Screenshot the exact section where it will live.
  2. Document container: dimensions, background, overlay gradient, blend mode, opacity.
  3. Write prompt using formula:
     [Subject] + [Material] + [Palette hex] + [Lighting] + [Angle] + [Background] + [Context] + [Negative]
  4. Generate 4 variants with Nano Banana Pro.
  5. Test best variant in situ — composite into page screenshot.
  6. Evaluate: blend / lighting match / feels native — a blind critic, not your self-report.
  7. If doesn't blend → adjust prompt → regenerate.
  8. Select with a written reason, verify, promote with provenance (engines/ledger.mjs).
     Never silently promote candidate 1.

Anti-Pattern #2 (Blind Generation): Complete checklist BEFORE every prompt, no exceptions.
Anti-Pattern #6 (Trinket Dropping): Test in situ BEFORE finalizing, no exceptions.
```

---

## Quick Enhancement (No Scroll Engine, No Artifacts)

```
Iron Canvas v6 SOLO — Quick structural upgrade for [URL].

Phases 1-4 only. No image generation. No scroll engine.

Deliverables:
  1. tokens.css — complete token system from DNA extraction (role tokens + RGB mirror triplets)
  2. Scroll reveal animations on all below-fold sections (.reveal + IntersectionObserver)
  3. 3-state micro-interactions on every interactive element (hover + active + focus)
  4. Role-token easing on all transitions — var(--ease-arrive) for every entrance and state
     change; var(--ease-tick) only for micro-moves ≤ 0.25 s
  5. Tinted shadows using site's own accent color
  6. Grain overlay (if creative/luxury project) — otherwise one other ambient layer
  7. Mobile responsive check at 375px
  8. The aliveness floor: one designed arrival (≤ 1.5 s), one heartbeat, a composed still
     under reduced motion

Estimated time: 2-4 hours.
Identity preservation rule: enhanced site must be recognizably the same brand.
```

---

## North Star Only (Inspiration Generation)

```
Iron Canvas Phase 3 SCOUT — Generate North Star reference image only.

Project: [description]
Project type: [A/B/C/D]
Feel profile: [Primary: ___] [Anti-feelings: ___] [Vibe: ___]
Palette: Dominant [#hex] Accent [#hex]
Brand personality: Bold [score] Avant-garde [score]

1. Search: [relevant queries based on project type and industry]
2. Find 3 reference sites matching the feel profile — one Voice, one World, one Instrument.
3. Generate North Star with Nano Banana Pro using the Type [X] prompt template
   (every hex, face and material from THIS project's DNA — never the studio's).
4. Validate against 5-point checklist.
5. Output: north-star-reference.png + scout-report.json
```

---
*Iron Canvas v6 — Island Development Crew*
