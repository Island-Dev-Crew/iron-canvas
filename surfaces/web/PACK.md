# Surface Pack: WEB — Iron Canvas v5.0
## *The flagship. The body of this pack is SKILL.md itself.*

```yaml
pack: web
surface: "Websites — animated sites, campaigns, portfolios, marketing/SaaS landing pages, e-commerce, enhancements, new builds"
project_types: [A, B, D, E, F]
status: stable
version: 5.0
```

This pack exists for contract compliance and router clarity. **It adds nothing to SKILL.md
and overrides nothing in it** — the entire v4.2 web pipeline (phases 0–8, all gates, all
agent prompts, all references) IS the web surface, fully inline per the self-containment law.

### 2. PHASE OVERRIDES
None. SKILL.md governs every phase as written.

### 3. DIS PROFILE
Standard — mode ceilings and BPM drivers exactly as `references/design-intensity-scale.md`
specifies for all 9 systems.
★v6: the aliveness floor sits under the DIS — arrival, heartbeat, hand-feel, breath and a composed
still ship at every register and every mode; no cap in this profile removes them.

**Power engines (★v6)** — gated by register, as `references/power-engines.md` specifies: R0/R1 all
off · R2 the signature only (Blender only at depth ≥ 0.4) · R3 on · R4 on + the twin camera. A
missing engine takes its named fallback; the web surface never blocks on one.

### 4. DEPTH PROFILE
Full range 0.1–1.0, gated by mode + BPM per `references/depth-language.md`.
Tier III (inhabited worlds) unlocks at `bold ≥ 8 AND avant_garde ≥ 8` in MISSION mode.
*(Sustained Tier III work — where the world IS the experience, not one hero moment —
dispatches to `surfaces/immersive-3d/` at Phase 0. This pack governs Tier III *moments*
inside otherwise-DOM pages, with depth-language.md as the authority and the §22 evidence
gate mandatory at depth ≥ 0.4.)*

### 5. VERIFY PROFILE
Standard Phase 7 battery: Lighthouse, LCP per performance tier, 7-axis audit
(incl. Axis 6 — CD3 Treatment & Soul: register match + anti-slop + subject-grounding;
★v6 Axis 7 — Aliveness, need 6/6, **never waived at any register**: arrival · heartbeat ·
hand-feel · breath · composed still · the score held — proven by the first-5-seconds video, the
15-second slow scroll and the reduced-motion recording), accessibility checklist, above-fold
conversion checks — as SKILL.md specifies.
★v6 score rules on this pack: one signature, one clock; mobile never pins at R0–R3 (pinned acts
declare a mobile transformation instead); R3/R4 budgets — first-load JS ≤ 180 kB gzipped with
three.js lazy, above-the-fold media ≤ 6 MB, one display + one text face, DPR 1.5 / 1.

### 6. NORTH STAR SEARCH SETS
As SKILL.md Phase 3 + the Tier III set in `references/depth-language.md` when depth ≥ 0.75.

### 7. OUTPUT ARTIFACTS DELTA
None. The § OUTPUT ARTIFACTS tree in SKILL.md is this pack's tree.

---

*Surface Pack: WEB — Iron Canvas v5.0*
*"The flagship surface. Everything else is built to its standard."*
